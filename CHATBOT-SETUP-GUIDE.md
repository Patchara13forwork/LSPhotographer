# คู่มือติดตั้ง Chatbot — ชมรมถ่ายภาพ โรงเรียนลาซาล

คู่มือนี้สำหรับติดตั้งแชทบอทถามตอบบนเว็บไซต์ (ใช้ Gemini API) ใช้เวลาประมาณ 15 นาที
พี่มี Gemini API key อยู่แล้ว ข้ามขั้นตอนสมัครไปได้เลย

---

## ภาพรวมระบบ

```
ผู้ใช้พิมพ์คำถามในเว็บ
      ↓
Google Apps Script (เก็บ Gemini API key ให้ปลอดภัย + ใส่ข้อมูลชมรมเป็น context)
      ↓
Gemini API (ประมวลผลคำถาม + context แล้วตอบกลับ)
      ↓
เว็บไซต์แสดงคำตอบในกล่องแชท
```

**ทำไมต้องมี Apps Script คั่นกลาง แทนที่จะเรียก Gemini ตรงจากเว็บ:**
ถ้าเรียก Gemini API ตรงจาก JavaScript ในเว็บ ใครก็ตามที่เปิด Developer Tools (กด F12)
จะเห็น API key ของพี่ทันที แล้วเอาไปใช้แทนได้ (ทำให้โควตา/ค่าใช้จ่ายของพี่ถูกคนอื่นใช้ฟรี)
การมี Apps Script เป็นตัวกลางทำให้ key ถูกเก็บไว้ฝั่งเซิร์ฟเวอร์ ไม่มีทางเห็นจากฝั่งเว็บได้เลย

---

## ขั้นที่ 1: สร้าง Google Apps Script ใหม่ (แยกจากตัวที่ทำ Sheet API)

1. ไปที่ https://script.google.com
2. กด **New project**
3. ลบโค้ดเปล่าเดิมทิ้งทั้งหมด
4. เปิดไฟล์ `chatbot-apps-script.gs` ที่แนบมา คัดลอกทั้งหมด แล้ววางแทน
5. หาบรรทัดนี้ใกล้ต้นไฟล์:
   ```js
   const GEMINI_API_KEY = "ใส่_GEMINI_API_KEY_ของพี่ตรงนี้";
   ```
   แก้เป็น API key จริงของพี่จาก Google AI Studio
6. กด **Save** (Ctrl+S) ตั้งชื่อโปรเจกต์ เช่น "Photo Club Chatbot API"

## ขั้นที่ 2: Deploy เป็น Web App

1. กด **Deploy** → **New deployment**
2. คลิกไอคอนเฟือง ⚙️ เลือก **Web app**
3. ตั้งค่า:
   - **Execute as**: **Me**
   - **Who has access**: **Anyone** (สำคัญ ต้องเลือกอันนี้)
4. กด **Deploy** → Authorize สิทธิ์ (เหมือนตอนทำ Sheet API — กด Advanced → Go to [ชื่อโปรเจกต์] (unsafe) → Allow)
5. คัดลอก **Web app URL** ที่ได้ (หน้าตาแบบ `https://script.google.com/macros/s/AKfycb.../exec`)

## ขั้นที่ 3: ทดสอบ backend ก่อนต่อกับเว็บ

เปิด URL ที่ได้จากขั้นที่ 2 ในเบราว์เซอร์ตรงๆ (วิธี GET) ควรเห็นข้อความ:
```json
{"status":"chatbot backend พร้อมใช้งาน — ส่ง POST request เพื่อถามคำถาม"}
```
ถ้าเห็นแบบนี้แปลว่า deploy สำเร็จแล้ว

## ขั้นที่ 4: ใส่ URL ลงในเว็บ

1. เปิดไฟล์ `index.html`
2. หาบรรทัดนี้ (อยู่ใกล้ท้ายไฟล์ ในส่วน CHATBOT WIDGET):
   ```js
   const CHATBOT_API_URL = "https://script.google.com/macros/s/YOUR-CHATBOT-DEPLOYMENT-ID/exec";
   ```
3. แทนที่ด้วย URL จริงจากขั้นที่ 2
4. บันทึกไฟล์ แล้วเปิดเว็บทดสอบ — จะเห็นปุ่มแชทวงกลมสีแดงมุมขวาล่าง

## ขั้นที่ 5: ทดสอบแชทบอทจริง

1. กดปุ่มแชทมุมขวาล่าง
2. ลองพิมพ์คำถาม เช่น "ต้องมีกล้องเองไหม" หรือกดปุ่มคำถามแนะนำที่แสดงไว้
3. รอสักครู่ (1-2 วินาที) ควรเห็นคำตอบจากบอท

---

## การแก้ไขข้อมูลที่บอทตอบ (สำคัญมาก — อ่านส่วนนี้)

บอทตอบตามข้อมูลใน **CLUB_CONTEXT** ที่อยู่ในไฟล์ `chatbot-apps-script.gs` เท่านั้น
ถ้าข้อมูลชมรมเปลี่ยน (เช่น เปลี่ยนเวลาประชุม เปลี่ยนช่องทางติดต่อ) ต้องมาแก้ตรงนี้ด้วยเสมอ
ไม่งั้นบอทจะตอบข้อมูลเก่า

**วิธีแก้:**
1. เปิด Apps Script ของ chatbot (ที่ script.google.com เข้าไปที่โปรเจกต์ "Photo Club Chatbot API")
2. แก้ข้อความในตัวแปร `CLUB_CONTEXT` ให้ตรงกับข้อมูลปัจจุบัน
3. กด Deploy → **Manage deployments** → กดไอคอนดินสอ → เปลี่ยน Version เป็น **New version** → **Deploy**
   (ต้องทำขั้นตอนนี้ทุกครั้งที่แก้โค้ด ไม่งั้นการแก้ไขจะไม่มีผลจริง)

---

## ข้อจำกัดและข้อควรระวัง

- **โควตาฟรีของ Gemini API มีจำกัด** — ถ้ามีคนถามพร้อมกันเยอะมากอาจถึงโควตาต่อวัน/ต่อนาที
  ตรวจสอบโควตาปัจจุบันได้ที่ https://aistudio.google.com (เมนู API keys)
- **บอทอาจตอบผิดได้เป็นบางครั้ง** (ธรรมชาติของ AI) จึงมีข้อความ disclaimer ใต้กล่องแชทไว้แล้ว
- **ประวัติแชทไม่ถูกบันทึกถาวร** — หายเมื่อผู้ใช้ปิด/รีเฟรชหน้าเว็บ (เป็นการตัดสินใจเพื่อความง่ายและความเป็นส่วนตัว ถ้าต้องการเก็บประวัติสามารถแจ้งให้ปรับเพิ่มได้)
- บอทถูกกำหนดให้ **ไม่ตอบนอกเหนือข้อมูลที่ให้ไว้** (ป้องกันการตอบมั่ว) ถ้าถามเรื่องที่ไม่เกี่ยวกับชมรม บอทจะแนะนำให้ติดต่อครูที่ปรึกษาหรือแอดมินแทน

---

มีปัญหาตรงไหนติดขัด ส่ง error message หรือ screenshot มาถามได้เลยครับ
