# คู่มือติดตั้งระบบจัดการงาน (Google Sheet) — ชมรมถ่ายภาพ โรงเรียนลาซาล

คู่มือนี้เขียนไว้ให้รุ่นน้องที่ไม่เคยเขียนโค้ดก็ทำตามได้ ใช้เวลาประมาณ 15-20 นาที
**ทำครั้งเดียว** — หลังจากนั้นการเพิ่ม/แก้ไขงานจะทำผ่าน Google Sheet ธรรมดาเลย
ไม่ต้องแตะโค้ดอีกต่อไป

---

## ภาพรวมระบบ (อ่านก่อนเริ่ม)

```
Google Sheet (ตารางเก็บข้อมูลงาน)
      ↓
Google Apps Script (สคริปต์เล็กๆ แปลง Sheet เป็นข้อมูลที่เว็บอ่านได้)
      ↓
เว็บไซต์ index.html (ดึงข้อมูลจากสคริปต์มาแสดง)
```

พูดง่ายๆ คือ: **แก้ Google Sheet → เว็บอัปเดตอัตโนมัติ** ไม่ต้องแก้โค้ดเว็บเลย

---

## ขั้นที่ 1: สร้าง Google Sheet

1. ไปที่ https://sheets.google.com แล้วสร้างสเปรดชีทใหม่ (กด "+" หรือ "Blank")
2. ตั้งชื่อไฟล์ เช่น "ฐานข้อมูลงาน ชมรมถ่ายภาพ"
3. คลิกขวาที่แท็บ "Sheet1" ด้านล่าง แล้วเลือก **Rename** เปลี่ยนชื่อเป็น `Events`
   (**ต้องสะกดตรงตัวใหญ่เล็กแบบนี้เป๊ะๆ** เพราะสคริปต์จะหาแท็บชื่อนี้)
4. ที่แถวแรก (แถวหัวตาราง) ให้พิมพ์หัวข้อคอลัมน์ตามนี้ **เรียงจากซ้ายไปขวาให้ตรงเป๊ะ**:

   | A | B | C | D | E | F | G | H |
   |---|---|---|---|---|---|---|---|
   | id | name | event_date | folder_url | thumbnail | description | tags | photo_count |

   และคอลัมน์ I:
   | I |
   |---|
   | featured |

5. เริ่มใส่ข้อมูลงานตั้งแต่แถวที่ 2 เป็นต้นไป ตัวอย่าง:

   | id | name | event_date | folder_url | thumbnail | description | tags | photo_count | featured |
   |----|------|-----------|-----------|-----------|--------------|------|-------------|----------|
   | e01 | งานกีฬาสี 2568 | 5 ส.ค. 2568 | https://drive.google.com/drive/folders/xxxxx | https://lh3.googleusercontent.com/d/xxxxx | ภาพกีฬาสีประจำปี | กีฬาสี, 2568, กีฬา | 387 | TRUE |

   **คำอธิบายแต่ละคอลัมน์:**
   - `id` — รหัสงาน ใส่อะไรก็ได้ที่ไม่ซ้ำกัน เช่น e01, e02, e03
   - `name` — ชื่องาน
   - `event_date` — วันที่ (พิมพ์เป็นข้อความธรรมดาได้เลย เช่น "5 ส.ค. 2568")
   - `folder_url` — ลิงก์โฟลเดอร์ Google Drive ของงานนั้น
   - `thumbnail` — ลิงก์รูปปก (ดูวิธีหาลิงก์รูปด้านล่าง)
   - `description` — คำอธิบายสั้นๆ
   - `tags` — คั่นด้วยจุลภาค `,` เช่น "กีฬาสี, 2568, กีฬา"
   - `photo_count` — จำนวนรูปในโฟลเดอร์ (ตัวเลขล้วนๆ)
   - `featured` — พิมพ์ `TRUE` ถ้าอยากให้ขึ้นเป็น "ผลงานตัวอย่าง" หน้าแรก หรือ `FALSE`/เว้นว่างถ้าไม่ต้องการ

   **วิธีหาลิงก์รูปปก (thumbnail):**
   1. เปิดรูปที่ต้องการใน Google Drive
   2. คลิกขวา → **Share** → เปลี่ยนเป็น **Anyone with the link** (ใครมีลิงก์ก็ดูได้)
   3. เอา File ID จาก URL (ส่วนที่อยู่ระหว่าง `/d/` กับ `/view`)
   4. ใส่ในรูปแบบ `https://lh3.googleusercontent.com/d/FILE_ID_ตรงนี้`

---

## ขั้นที่ 2: ติดตั้ง Apps Script (ทำครั้งเดียว)

1. ใน Google Sheet ที่สร้างไว้ ไปที่เมนู **Extensions** → **Apps Script**
2. จะเปิดหน้าต่างใหม่ขึ้นมา มีโค้ดเปล่าๆ อยู่ (`function myFunction() {}`)
3. **ลบโค้ดเปล่านั้นทิ้งทั้งหมด**
4. เปิดไฟล์ `apps-script-code.gs` ที่แนบมาให้ คัดลอกทั้งหมด แล้ววางแทน
5. กด **Save** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S) — อาจให้ตั้งชื่อโปรเจกต์ ตั้งอะไรก็ได้ เช่น "Photo Club API"

## ขั้นที่ 3: Deploy เป็น Web App (ทำครั้งเดียว)

1. ที่มุมขวาบนของหน้า Apps Script กด **Deploy** → **New deployment**
2. คลิกไอคอนเฟือง ⚙️ ข้างคำว่า "Select type" แล้วเลือก **Web app**
3. ตั้งค่าดังนี้:
   - **Description**: ใส่อะไรก็ได้ เช่น "v1"
   - **Execute as**: เลือก **Me** (บัญชีของคนที่ deploy)
   - **Who has access**: เลือก **Anyone** (สำคัญมาก — ต้องเลือกอันนี้ ไม่งั้นเว็บจะดึงข้อมูลไม่ได้)
4. กด **Deploy**
5. Google จะขอให้ authorize สิทธิ์ — กด **Authorize access** แล้วเลือกบัญชี Google ของคุณ
   - อาจมีหน้าเตือน "Google hasn't verified this app" ให้กด **Advanced** → **Go to [ชื่อโปรเจกต์] (unsafe)**
     (ไม่มีอันตราย เพราะเป็นสคริปต์ที่คุณเขียน/รันเองเท่านั้น)
   - กด **Allow**
6. หลัง deploy สำเร็จ จะได้ **Web app URL** หน้าตาแบบนี้:
   ```
   https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxxxxxxx/exec
   ```
   **คัดลอก URL นี้เก็บไว้** จะใช้ในขั้นถัดไป

---

## ขั้นที่ 4: ใส่ URL ลงในเว็บ

1. เปิดไฟล์ `index.html`
2. หาบรรทัดนี้ (อยู่ในส่วน `<script>` ใกล้ท้ายไฟล์):
   ```js
   const SHEET_API_URL = "https://script.google.com/macros/s/YOUR-DEPLOYMENT-ID/exec";
   ```
3. แทนที่ด้วย URL จริงที่คัดลอกมาจากขั้นที่ 3
4. บันทึกไฟล์ แล้วเปิดเว็บทดสอบ (เปิดไฟล์ในเบราว์เซอร์ หรือ deploy ขึ้นโฮสติ้งก่อน)
5. ถ้าเห็นงานที่ใส่ไว้ใน Sheet ขึ้นมาในเว็บ แปลว่าสำเร็จ

---

## การใช้งานหลังติดตั้งเสร็จ (สำหรับรุ่นน้องทุกคน)

**เพิ่มงานใหม่:** เปิด Google Sheet → เพิ่มแถวใหม่ด้านล่าง → กรอกข้อมูลตามคอลัมน์ → เสร็จ
เว็บจะแสดงงานใหม่ทันทีที่มีคนเข้าเว็บ (ไม่ต้อง deploy ซ้ำ ไม่ต้องแก้โค้ด)

**แก้ไขงานเดิม:** แก้ข้อมูลในแถวนั้นตรงๆ ได้เลย

**ลบงาน:** ลบทั้งแถวออก หรือลบแค่ค่าในช่อง `name` ก็พอ (ระบบจะข้ามแถวที่ไม่มีชื่องาน)

**ให้คนอื่นแก้ไขได้:** กด **Share** ที่มุมขวาบนของ Google Sheet แล้วใส่อีเมล Gmail ของสมาชิกที่ไว้ใจ
ตั้งสิทธิ์เป็น **Editor**

---

## ข้อควรระวัง

- **ห้ามเปลี่ยนชื่อหัวคอลัมน์** (แถวที่ 1) เพราะสคริปต์อ้างอิงชื่อคอลัมน์เป๊ะๆ ถ้าอยากเพิ่มคอลัมน์ใหม่ที่ไม่เกี่ยวกับระบบ ให้เพิ่มต่อทางขวาของ `featured` ได้ ระบบจะไม่สนใจคอลัมน์เกิน
- **ห้ามเปลี่ยนชื่อแท็บ** จาก `Events` เป็นชื่ออื่น ถ้าจำเป็นต้องเปลี่ยนจริงๆ ต้องไปแก้ค่า `SHEET_NAME` ในไฟล์ Apps Script ด้วย
- ถ้าแก้โครงสร้าง Sheet เยอะแล้วเว็บไม่ขึ้นข้อมูล ลองเปิด Console ของเบราว์เซอร์ (กด F12) ดู error message

---

## ถ้าอยากเปลี่ยนโค้ด Apps Script ในอนาคต

ทุกครั้งที่แก้โค้ดใน Apps Script editor แล้วอยากให้มีผลจริง ต้อง:
1. Deploy → **Manage deployments**
2. กดไอคอนดินสอ (Edit) ที่ deployment เดิม
3. เปลี่ยน Version เป็น **New version**
4. กด **Deploy** อีกครั้ง (URL จะเหมือนเดิม ไม่ต้องเปลี่ยนใน index.html)

---

มีปัญหาตรงไหนติดขัด ถ่าย screenshot ของ error มาถามได้เลยครับ
